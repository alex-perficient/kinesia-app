import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Prueba de control inicial (Dummy Test) para CI/CD', () {
    // Si 1 + 1 es igual a 2, la prueba pasa y el robot de GitHub continúa.
    expect(1 + 1, 2);
  });
}